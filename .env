# ZeroSSL 访问密钥（控制台 Developer 里拿）
ZEROSSL_KEY="你的_access_key"

# 目标公网 IP（要签发证书的那个 IP）
IP="1.2.3.4"

# 有付费年证书配额就设 365；否则用 90
VALID_DAYS=90

# Web 根（校验文件会写在 $WEBROOT/.well-known/pki-validation/）
WEBROOT="/var/www"

# 证书安装目录（会自动创建） 和 caddy 配置的目录一直
INSTALL_DIR="/usr/share/caddy/zerossl-${IP}"

# 证书到期前多少天开始续期
RENEW_BEFORE_DAYS=40

# 是否每次续期更换私钥（1=每次换新；0=沿用原私钥）
ROTATE_KEY=0

# 成功后执行的重载命令（按你的环境二选一）
POST_RELOAD_CMD="systemctl reload nginx"
# POST_RELOAD_CMD="caddy reload"    # 如果用 Caddy
